<?php/** * ========================= * Appointments Path * ========================= */$file_name = array(    'apt-menu',    'show-data',    'create-apt',    'apt-trans',    'apt-setting',    'past-appointment',);define('DASHBOARD_PATH', APTINK_CLASS . 'appointments-form/apt-dashboard/');define('DATABASE_PATH', APTINK_CLASS . 'appointments-form/database/');define('APT_CLASS', APTINK_CLASS . 'appointments-form/apt-class/');define('ABS_URL_PATH', plugins_url('apt-dashboard/apt-toolltip/', __FILE__));// custom page path and callforeach ($file_name as $files) {    if (file_exists(DASHBOARD_PATH . $files . '.php')):        require_once(DASHBOARD_PATH . $files . '.php');    endif;}if (file_exists(DATABASE_PATH . 'apt-database.php')):    require_once(DATABASE_PATH . 'apt-database.php');endif;if (file_exists(APT_CLASS . 'apt-class.php')):    require_once(APT_CLASS . 'apt-class.php');endif;if (file_exists(APT_CLASS . 'ajax-function.php')):    require_once(APT_CLASS . 'ajax-function.php');endif;?>